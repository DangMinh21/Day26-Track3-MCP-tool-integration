# Bearer token for HTTP transport (bonus). Required when --transport http is used.
MCP_AUTH_TOKEN=devtoken123

# Optional override for SQLite database path. Defaults to ./lab.db next to mcp_server.py
# DB_PATH=./lab.db
